image:
  file: .gitpod.Dockerfile

tasks:
  - init: yes | lamdera reset
    command: sudo lamdera live
ports:
  - port: 8000
    onOpen: open-preview

vscode:
  extensions:
    - elmtooling.elm-ls-vscode
    - hbenl.vscode-test-explorer
    - hbenl.test-adapter-converter
