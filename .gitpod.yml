image:
  file: .gitpod.Dockerfile

tasks:
  - init: lamdera reset --Y
    command: sudo lamdera live
ports:
  - port: 8000
    onOpen: open-preview

vscode:
  extensions:
    - elmtooling.elm-ls-vscode
